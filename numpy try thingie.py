from asyncore import read
from tkinter import *
from datetime import *
import numpy as np

root = Tk()








events=Label(root, text=)
title=Label(root, text="Brent's countdown calender")


events.grid(row=1, column=0)
title.grid(row=0, column=0)

root.mainloop()
